spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://mysql_db:3306/countries 
    username: root
    password: root 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      #format_sql: true
      #dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false